# Build-time ONLY — run locally to generate knowledge_base.json.
# The Lambda does NOT need these at runtime; it only reads the JSON output.
pymupdf>=1.24     # PDF -> text extraction (imported as `fitz`)
openai>=1.50      # embeddings API (text-embedding-3-small)
